import React from "react";

const Header = ()=>{
    return (
        <header>
            <img src="../asset/logo.png"></img>
            <nav>

            </nav>
        </header>
    );
};

export default Header;