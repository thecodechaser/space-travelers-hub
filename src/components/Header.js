import React from "react";
import logo from '../asset/logo.png';

const Header = ()=>{
    return (
        <header>
            <img alt= "logo-top" src={logo} className="logo-top"></img>
            <nav>
            </nav>
        </header>
    );
};

export default Header;